# Template -- do NOT commit with real values.
# Prefer `k3sc deploy`, which creates/updates this secret from local auth sources.
# In-cluster auth should flow only through secret-backed env vars, not hostPath token files.
apiVersion: v1
kind: Secret
metadata:
  name: claude-secrets
  namespace: claude-agents
type: Opaque
stringData:
  CLAUDE_CODE_OAUTH_TOKEN: "REPLACE_ME"
  GITHUB_TOKEN: "REPLACE_ME"
  OPENAI_API_KEY: ""
  CODEX_AUTH_JSON: ""
  ANTHROPIC_API_KEY: ""
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
  AWS_REGION: ""
  CLAUDE_CODE_USE_BEDROCK: ""
